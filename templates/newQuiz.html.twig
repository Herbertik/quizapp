{% extends 'base.html.twig' %}

{% block title %}Quiz App{% endblock %}

{% block body %}
    <style>
        .example-wrapper code { background: #0000ff; padding: 2px 6px; }
    </style>
    {{ form_start(form) }}

    {{ form_end(form) }}

    <a href="{{ path('mainMenu', {'limit': '10'}) }}">to menu</a> <input type="submit">
{% endblock %}
